/** Jolpica F1 fetch and CDN cache durations (seconds). */
export const F1_CACHE = {
  SCHEDULE: 3600,
  STANDINGS: 300,
  EQUIVALENT_RACE: 3600,
  SCHEDULE_S_MAXAGE: 3600,
  SCHEDULE_STALE_WHILE_REVALIDATE: 7200,
  STANDINGS_S_MAXAGE: 300,
  STANDINGS_STALE_WHILE_REVALIDATE: 600,
  EQUIVALENT_RACE_S_MAXAGE: 3600,
  EQUIVALENT_RACE_STALE_WHILE_REVALIDATE: 7200,
} as const;
